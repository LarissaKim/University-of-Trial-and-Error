<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title }}</title>
    <style>
      body {
        text-align: center;
        background: #1d1e22;
        color: #dddcdd;
        font-family: monospace;
      }

      a {
        color: #dddcdd;
        opacity: 0.8;
      }

      a:hover {
        color: #e30b5d;
        opacity: 1;
      }

      div.container {
        width: 400px;
        margin: 0 auto;
      }

      .honeypot {
        font-size: 4rem;
      }
    </style>
  </head>

  <body>
    <header><a href="{{ '/' | url }}">For you</a> | <a href="{{ '/about' | url }}">About this page for you</a></header>
    {{ content | safe }}
  </body>
</html>
